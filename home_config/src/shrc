export SUDO_ASKPASS="${HOME}/SUDO_ASKPASS"
export EDITOR='hx'

test -e  "${HOME}/.cargo/env" && . "${HOME}/.cargo/env"
test -e  "${HOME}/important_functions.sh" && . "${HOME}/important_functions.sh"

y() {
	yazi \
		--chooser-file "/tmp/yazi-chooser-${$}" \
		--cwd-file "/tmp/yazi-cwd-${$}" \
	;

	cd "$(cat "/tmp/yazi-cwd-${$}")"

	rm -f -- \
		 "/tmp/yazi-chooser-${$}" \
		 "/tmp/yazi-cwd-${$}" \
	;
}

