* Main working script
#+begin_src sh :shebang #!/bin/sh :results output :mkdirp yes :tangle ./.git.sh
  . "${HOME}/important_functions.sh"

  CLEAN '.git.sh'
  CLEAN 'main.org~'

  GITADD 'Cargo.toml'
  GITADD 'host.image_names.sh'
  GITADD 'image_run.sh'
  GITADD 'main.org'
  GITADD 'src/alacritty.toml'
  GITADD 'src/bashrc'
  GITADD 'src/fish_config.fish'
  GITADD 'src/foot.ini'
  GITADD 'src/helix_config.toml'
  GITADD 'src/main.rs'
  GITADD 'src/shrc'
  GITADD 'src/wezterm.lua'
  GITADD 'src/zshrc'
#+end_src

* run the script
#+begin_src emacs-lisp :results silent
  (save-buffer)
  (org-babel-tangle)
  (async-shell-command "
      './.git.sh'
  " "log" "err")
#+end_src
