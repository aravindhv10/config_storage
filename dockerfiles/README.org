* Files to ignore
#+begin_src conf :mkdirp yes :tangle ./.gitignore
#+end_src

* Main build script
#+begin_src sh :shebang #!/bin/sh :results output :mkdirp yes :tangle ./build_container.sh
  cd "$('dirname' -- "${0}")"
  ln -vfs '../shell_functions/important_functions.sh' './'
  cp './important_functions.sh' "./${1}/"
  cd "./${1}/"
  IMAGE_NAME="$(basename -- "${1}")"
  mkdir -pv -- './build'
  H="$(cat './Dockerfile' './important_functions.sh' | sha512sum | cut -d ' ' -f1)"
  test -e "./build/${H}" && exit '0'
  CMD='sudo -A docker'
  which buildah && CMD='buildah'
  ${CMD} build -t "${IMAGE_NAME}" -f './Dockerfile' . && touch "./build/${H}"
  exit '0'
#+end_src

* Actual dockerfiles

** 0_base

*** CPU
#+begin_src conf :mkdirp yes :tangle ./CPU/0_base/Dockerfile
  FROM debian:testing-backports
#+end_src

*** CUDA_UBUNTU
#+begin_src conf :mkdirp yes :tangle ./CUDA_UBUNTU/0_base/Dockerfile
  FROM nvidia/cuda:13.2.1-cudnn-devel-ubuntu24.04
#+end_src

*** AMDGPU

**** Main base image
#+begin_src conf :mkdirp yes :tangle ./AMDGPU_PYT/0_base/Dockerfile
  FROM rocm/dev-ubuntu-24.04:7.2.3-complete
#+end_src

**** COMMENT provide default python venv for use later
#+begin_src conf :mkdirp yes :tangle ./AMDGPU_PYT/0_base/Dockerfile
  RUN ln -vfs -- '../opt/venv' '/root/venv'
#+end_src

**** COMMENT Copy important functions script
#+begin_src conf :mkdirp yes :tangle ./AMDGPU_PYT/0_base/Dockerfile
  COPY './important_functions.sh' '/root/important_functions.sh'
#+end_src

**** COMMENT Install essential rocm packages
#+begin_src conf :mkdirp yes :tangle ./AMDGPU_PYT/0_base/Dockerfile
  RUN \
      --mount=target=/var/lib/apt/lists,type=cache,sharing=locked \
      --mount=target=/var/cache/apt,type=cache,sharing=locked \
      echo 'START apt-get update' \
      && . '/root/important_functions.sh' \
      && get_amd_rocm_packages_ubuntu \
      && echo 'DONE apt-get update' ;
#+end_src

** 1_install_packages

*** Base image
#+begin_src conf :mkdirp yes :tangle ./CPU/1_install_packages/Dockerfile
  FROM 0_base
#+end_src

*** Important env
#+begin_src conf :mkdirp yes :tangle ./CPU/1_install_packages/Dockerfile
  ENV HOME='/root'
  ENV DEBIAN_FRONTEND='noninteractive'
  ENV CMAKE_EXPORT_COMPILE_COMMANDS=1
  WORKDIR '/root'
  USER root
#+end_src

*** Copy important functions
#+begin_src conf :mkdirp yes :tangle ./CPU/1_install_packages/Dockerfile
  COPY './important_functions.sh' '/root/important_functions.sh'
#+end_src

*** Install common packages
#+begin_src conf :mkdirp yes :tangle ./CPU/1_install_packages/Dockerfile
  RUN \
      --mount=target=/var/lib/apt/lists,type=cache,sharing=locked \
      --mount=target=/var/cache/apt,type=cache,sharing=locked \
      echo 'START apt-get update' \
      && apt-get update \
      && . '/root/important_functions.sh' \
      && get_apt_packages \
      && echo 'DONE apt-get update' ;
#+end_src

** 2_install_rust

*** Base image
#+begin_src conf :mkdirp yes :tangle ./CPU/2_install_rust/Dockerfile
  FROM 1_install_packages
#+end_src

*** Copy important functions
#+begin_src conf :mkdirp yes :tangle ./CPU/2_install_rust/Dockerfile
  COPY './important_functions.sh' '/root/important_functions.sh'
#+end_src

*** Setting env
#+begin_src conf :mkdirp yes :tangle ./CPU/2_install_rust/Dockerfile
  ENV CARGO_HOME="${HOME}/cargo"
  ENV RUSTUP_HOME="${HOME}/rustup"
  ENV PATH="${HOME}/bin:${CARGO_HOME}/bin:${PATH}"
#+end_src

*** Download and install rust
#+begin_src conf :mkdirp yes :tangle ./CPU/2_install_rust/Dockerfile
  RUN \
      --mount=target=/root/SHA512SUM,type=cache \
      --mount=target=/root/TMP,type=cache \
      echo 'START Download and install rust' \
      && . "${HOME}/important_functions.sh" \
      && adown \
          'https://sh.rustup.rs' \
          'rustup.rs' \
          '494ddf67a104d5deb49380968ecccd8648ee0d7a3e61a8fa45b4dfd3bdc51ba8777922ed0e1864bf81ba78ce718bd40c55c1835c328df3cd1cd0d17312411460' \
          "${HOME}/rustup-init.sh" \
      && cd "${HOME}" \
      && chmod +x 'rustup-init.sh' \
      && './rustup-init.sh' '-y' '--no-modify-path' \
      && echo 'DONE Download and install rust'
#+end_src

*** Install additional rust components
#+begin_src conf :mkdirp yes :tangle ./CPU/2_install_rust/Dockerfile
  RUN rustup default stable
  RUN rustup component add rust-src
  RUN rustup component add rust-analyzer
  RUN rustup component add rustfmt
#+end_src

*** Download and install zig
#+begin_src conf :mkdirp yes :tangle ./CPU/2_install_rust/Dockerfile
  RUN \
      --mount=target=/root/SHA512SUM,type=cache \
      --mount=target=/root/TMP,type=cache \
      echo 'START Download and install zig' \
      && . "${HOME}/important_functions.sh" \
      && adown \
          'https://ziglang.org/builds/zig-x86_64-linux-0.17.0-dev.1941+71115f0ab.tar.xz' \
          'zig-x86_64-linux-0.17.0-dev.1941+71115f0ab.tar.xz' \
          'ac0b7866a539299083bb74cf1ae4ecdb90a9e33d81241f61ef5892dc5448211c63fd1f3afc335cba9b0dd3557d39adb73d9dd92948e5a37004d3be13a5a4359c' \
          "${HOME}/ZIG/zig-x86_64-linux-0.17.0-dev.1941+71115f0ab.tar.xz" \
      && cd "${HOME}/ZIG/" \
      && tar -xf './zig-x86_64-linux-0.17.0-dev.1941+71115f0ab.tar.xz' \
      && cd './zig-x86_64-linux-0.17.0-dev.1941+71115f0ab' \
      && mv * /usr/local/bin/ \
      && echo 'DONE Download and install zig' ;
#+end_src

** 3_good_setup

*** The base image
#+begin_src conf :mkdirp yes :tangle ./CPU/3_good_setup/Dockerfile
  FROM 2_install_rust
#+end_src

*** Copy important functions shell script
#+begin_src conf :mkdirp yes :tangle ./CPU/3_good_setup/Dockerfile
  COPY './important_functions.sh' '/root/important_functions.sh'
#+end_src

*** Configure zsh and other stuff
#+begin_src conf :mkdirp yes :tangle ./CPU/3_good_setup/Dockerfile
  RUN \
      --mount=target=/root/SHA512SUM,type=cache \
      --mount=target=/root/TMP,type=cache \
      echo 'START Download and install rust programs' \
      && . "${HOME}/important_functions.sh" \
      && get_all_good_programs_and_config \
      && echo 'DONE Download and install rust programs' ;

  RUN \
      echo 'START Delete tar files after installing' \
      fd '\.tar$' '/var/tmp' -t f -x unlink '{}' \
      echo 'DONE Delete tar files after installing' ;
#+end_src

** 4_python

*** The base image
#+begin_src conf :mkdirp yes :tangle ./CPU/4_python/Dockerfile
  FROM 3_good_setup
#+end_src

*** Setup venv if it does not already exist
#+begin_src conf :mkdirp yes :tangle ./CPU/4_python/Dockerfile
  RUN test -e "${HOME}/venv" || uv venv "${HOME}/venv"
  ENV PATH="/root/venv/bin:${PATH}"
  ENV VIRTUAL_ENV="${HOME}/venv"
  ENV VIRTUAL_ENV_PROMPT='venv'
#+end_src

*** Copy important functions
#+begin_src conf :mkdirp yes :tangle ./CPU/4_python/Dockerfile
  COPY './important_functions.sh' '/root/important_functions.sh'

  RUN \
      echo 'START pip' \
      && . "${HOME}/venv/bin/activate" \
      && uv pip install -U \
          'ipython' \
          'ninja' \
          'packaging' \
          'pip' \
          'setuptools' \
          'wheel' \
      && echo 'DONE pip' ;
#+end_src

*** COMMENT Download important codes
#+begin_src conf :mkdirp yes :tangle ./CPU/4_python/Dockerfile
  RUN \
      echo 'START Download all sources' \
      && . "${HOME}/important_functions.sh" \
      && get_repo 'https://github.com/facebookresearch/pytorchvideo.git' \
      && echo 'START Download all sources' ;

  RUN \
      echo 'START Download all sources' \
      && . "${HOME}/important_functions.sh" \
      && get_repo 'https://github.com/huggingface/diffusers.git' \
      && echo 'START Download all sources' ;

  RUN \
      echo 'START Download all sources' \
      && . "${HOME}/important_functions.sh" \
      && get_repo 'https://github.com/huggingface/transformers.git' \
      && git checkout 'tags/v4.57.3' \
      && echo 'START Download all sources' ;

  RUN \
      echo 'START Download all sources' \
      && . "${HOME}/important_functions.sh" \
      && get_repo 'https://github.com/huggingface/huggingface_hub.git' \
      && git checkout 'tags/v0.36.0' \
      && echo 'START Download all sources' ;

  RUN \
      echo 'START Download all sources' \
      && . "${HOME}/important_functions.sh" \
      && get_repo 'https://github.com/huggingface/pytorch-image-models.git' \
      && echo 'START Download all sources' ;

  RUN \
      echo 'START Download all sources' \
      && . "${HOME}/important_functions.sh" \
      && get_repo 'https://github.com/IDEA-Research/detrex.git' \
      && echo 'START Download all sources' ;
#+end_src

** 5_libtorch

*** CPU
#+begin_src conf :mkdirp yes :tangle ./CPU/5_libtorch/Dockerfile
  FROM 4_python

  COPY './important_functions.sh' '/root/important_functions.sh'

  RUN \
      echo 'START pip' \
      && . "${HOME}/venv/bin/activate" \
      && uv pip install \
          '--index-url' 'https://download.pytorch.org/whl/cpu' \
              'torch' \
              'torchvision' \
      && echo 'DONE pip' ;

  ENV LIBTORCH='/root/venv/lib/python3.14/site-packages/torch/'
  ENV CMAKE_PREFIX_PATH="${LIBTORCH}/share/cmake"
  ENV LD_LIBRARY_PATH="${LIBTORCH}/lib:${LD_LIBRARY_PATH}"
#+end_src

*** CUDA_UBUNTU
#+begin_src conf :mkdirp yes :tangle ./CUDA_UBUNTU/5_libtorch/Dockerfile
  FROM 4_python

  COPY './important_functions.sh' '/root/important_functions.sh'

  RUN \
      echo 'START pytorch' \
      && . "${HOME}/venv/bin/activate" \
      && uv pip install \
          '--extra-index-url' 'https://download.pytorch.org/whl/cu132' \
              'torch' \
              'torchvision' \
              'torchao' \
              'xformers' \
      && echo 'DONE pytorch' ;

  ENV LIBTORCH='/root/venv/lib/python3.12/site-packages/torch'
  ENV CMAKE_PREFIX_PATH="${LIBTORCH}/share/cmake"
  ENV LD_LIBRARY_PATH="${LIBTORCH}/lib:${LD_LIBRARY_PATH}"
#+end_src

*** AMDGPU
#+begin_src conf :mkdirp yes :tangle ./AMDGPU_PYT/5_libtorch/Dockerfile
  FROM 4_python

  COPY './important_functions.sh' '/root/important_functions.sh'

  RUN \
      echo 'START pytorch' \
      && . "${HOME}/venv/bin/activate" \
      && uv pip install \
          '--extra-index-url' 'https://download.pytorch.org/whl/rocm7.2' \
              'torch' \
              'torchvision' \
              'torchao' \
              'xformers' \
      && echo 'DONE pytorch' ;

  ENV LIBTORCH='/root/venv/lib/python3.12/site-packages/torch'
  ENV CMAKE_PREFIX_PATH="${LIBTORCH}/share/cmake"
  ENV LD_LIBRARY_PATH="${LIBTORCH}/lib:${LD_LIBRARY_PATH}"
#+end_src

** 6_common_python_data
#+begin_src conf :mkdirp yes :tangle ./CPU/6_common_python_data/Dockerfile
  FROM 5_libtorch

  COPY './important_functions.sh' '/root/important_functions.sh'

  RUN \
      echo 'START hashing' \
      && . "${HOME}/venv/bin/activate" \
      && uv pip install -U \
          'blake3' \
      && echo 'DONE hashing' ;

  RUN \
      echo 'START dataframe' \
      && . "${HOME}/venv/bin/activate" \
      && uv pip install -U \
          'datafusion' \
          'fastexcel' \
          'openpyxl' \
          'pandas' \
          'polars' \
          'python-calamine' \
      && echo 'DONE dataframe' ;

  RUN \
      echo 'START lancedb' \
      && . "${HOME}/venv/bin/activate" \
      && uv pip install -U \
          'lancedb' \
          'pylance' \
      && echo 'DONE lancedb' ;

  RUN \
      echo 'START others' \
      && . "${HOME}/venv/bin/activate" \
      && uv pip install -U \
          'fastapi[standard]' \
          'inotify-simple' \
          'ipywidgets' \
          'jupyter' \
          'jupyterlab' \
          'psutil' \
          'requests' \
          'scikit-learn' \
          'starlette' \
          'tqdm' \
          'urllib3' \
          'wandb' \
      && echo 'DONE others' ;

  RUN \
      echo 'START plotting' \
      && . "${HOME}/venv/bin/activate" \
      && uv pip install -U \
          'matplotlib' \
          'seaborn' \
      && echo 'DONE plotting' ;

  RUN \
      echo 'START image and tensor stuff' \
      && . "${HOME}/venv/bin/activate" \
      && uv pip install \
          'albumentationsx' \
          'einops' \
          'opencv_contrib_python' \
          'opencv_python' \
          'openvino' \
          'pillow' \
          'safetensors' \
          'sentencepiece' \
      && echo 'DONE image and tensor stuff' ;

  RUN \
      echo 'START GRPC stuff' \
      && . "${HOME}/venv/bin/activate" \
      && uv pip install \
          'grpcio' \
          'grpcio-tools' \
          'protobuf' \
      && echo 'DONE GRPC stuff' ;
#+end_src

** 7_deep_learning

*** CPU
#+begin_src conf :mkdirp yes :tangle ./CPU/7_deep_learning/Dockerfile
  FROM 6_common_python_data

  COPY './important_functions.sh' '/root/important_functions.sh'

  RUN \
      echo 'START timm' \
      && . "${HOME}/venv/bin/activate" \
      && uv pip install \
          'timm' \
      && echo 'DONE timm' ;

  RUN \
      echo 'START onnxstuff' \
      && . "${HOME}/venv/bin/activate" \
      && uv pip install \
          'onnxruntime' \
          'onnxscript' \
      && echo 'DONE onnxstuff' ;
#+end_src

*** CUDA_UBUNTU
#+begin_src conf :mkdirp yes :tangle ./CUDA_UBUNTU/7_deep_learning/Dockerfile
  FROM 6_common_python_data

  COPY './important_functions.sh' '/root/important_functions.sh'

  RUN \
      echo 'START Quantization, optimization and offloading' \
      && . "${HOME}/venv/bin/activate" \
      && uv pip install \
              'accelerate' \
              'datasets' \
              'deepspeed' \
              'diffusers' \
              'evaluate' \
              'huggingface_hub' \
              'lightning[extra]' \
              'onnxruntime-gpu' \
              'onnxscript' \
              'optimum' \
              'optimum-quanto' \
              'prodigyopt' \
              'timm' \
              'torch_dct' \
              'torchmetrics' \
              'transformers' \
              'ultralytics' \
      && echo 'DONE onnxstuff' ;
#+end_src

**** COMMENT Shaky parts
#+begin_src conf :mkdirp yes :tangle ./CUDA_UBUNTU/6_pytorch/Dockerfile
  RUN \
      echo 'START Quantization, optimization and offloading' \
      && . "${HOME}/venv/bin/activate" \
      && uv pip install \
              -e "${HOME}/GITHUB/facebookresearch/pytorchvideo" \
              -e "${HOME}/GITHUB/huggingface/diffusers" \
              -e "${HOME}/GITHUB/huggingface/huggingface_hub" \
              -e "${HOME}/GITHUB/huggingface/pytorch-image-models" \
              -e "${HOME}/GITHUB/huggingface/transformers" \
      && echo 'DONE onnxstuff' ;

  RUN \
      echo 'START Quantization, optimization and offloading' \
      && . "${HOME}/venv/bin/activate" \
      && uv pip install \
          '--no-build-isolation' \
              -e "${HOME}/GITHUB/IDEA-Research/detrex" \
              -e "${HOME}/GITHUB/IDEA-Research/detrex/detectron2" \
      && echo 'DONE onnxstuff' ;
#+end_src

*** AMDGPU
#+begin_src conf :mkdirp yes :tangle ./AMDGPU_PYT/7_deep_learning/Dockerfile
  FROM 6_common_python_data

  COPY './important_functions.sh' '/root/important_functions.sh'

  RUN \
      echo 'START Quantization, optimization and offloading' \
      && . "${HOME}/venv/bin/activate" \
      && uv pip install \
          'accelerate' \
          'datasets' \
          'deepspeed' \
          'diffusers' \
          'evaluate' \
          'huggingface_hub' \
          'lightning[extra]' \
          'onnxruntime-gpu' \
          'onnxscript' \
          'optimum' \
          'optimum-quanto' \
          'prodigyopt' \
          'timm' \
          'torch_dct' \
          'torchmetrics' \
          'transformers' \
          'ultralytics' \
      && echo 'DONE onnxstuff' ;
#+end_src

**** COMMENT Shaky parts
#+begin_src conf :mkdirp yes :tangle ./AMDGPU_PYT/6_pytorch/Dockerfile
  RUN \
      echo 'START Quantization, optimization and offloading' \
      && . "${HOME}/venv/bin/activate" \
      && uv pip install \
              -e "${HOME}/GITHUB/facebookresearch/pytorchvideo" \
              -e "${HOME}/GITHUB/huggingface/diffusers" \
              -e "${HOME}/GITHUB/huggingface/huggingface_hub" \
              -e "${HOME}/GITHUB/huggingface/pytorch-image-models" \
              -e "${HOME}/GITHUB/huggingface/transformers" \
      && echo 'DONE onnxstuff' ;

  RUN \
      echo 'START Quantization, optimization and offloading' \
      && . "${HOME}/venv/bin/activate" \
      && uv pip install \
          '--no-build-isolation' \
              -e "${HOME}/GITHUB/IDEA-Research/detrex" \
              -e "${HOME}/GITHUB/IDEA-Research/detrex/detectron2" \
      && echo 'DONE onnxstuff' ;
#+end_src

** 8_coding_setup
#+begin_src conf :mkdirp yes :tangle ./CPU/8_coding_setup/Dockerfile
  FROM 7_deep_learning

  COPY './important_functions.sh' '/root/important_functions.sh'

  RUN \
      --mount=target=/root/SHA512SUM,type=cache \
      --mount=target=/root/TMP,type=cache \
      echo 'START Download and install rust programs' \
      && . "${HOME}/important_functions.sh" \
      && get_all_good_programs_and_config \
      && echo 'DONE Download and install rust programs' ;

  RUN \
      --mount=target=/root/SHA512SUM,type=cache \
      --mount=target=/root/TMP,type=cache \
      echo 'START download llm-ls' \
      && . "${HOME}/important_functions.sh" \
      && adown \
          'https://github.com/huggingface/llm-ls/releases/download/0.5.3/llm-ls-x86_64-unknown-linux-gnu.gz' \
          'llm-ls-x86_64-unknown-linux-gnu.gz' \
          '28496e9b0729e981544c8d6d3f92b4a66cf1627f6ef3c97512b1e1af2b36f8cdcb598c36a72c40cfcdb6fbd65038b3315ae834f380ee44a268cd8d937b86bc1d' \
          "${HOME}/LSP/llm-ls-x86_64-unknown-linux-gnu.gz" \
      && gzip -df "${HOME}/LSP/llm-ls-x86_64-unknown-linux-gnu.gz" \
      && chmod +x "${HOME}/LSP/llm-ls-x86_64-unknown-linux-gnu" \
      && mv -vf -- \
          "${HOME}/LSP/llm-ls-x86_64-unknown-linux-gnu" \
          '/usr/local/bin/llm-ls' \
      && echo 'DONE download llm-ls' ;
#+end_src

* Script to build images

** CPU
#+begin_src sh :shebang #!/bin/sh :results output :tangle ./build_cpu.sh
  cd "$(dirname -- "${0}")"
  './build_container.sh' 'CPU/0_base'
  './build_container.sh' 'CPU/1_install_packages'
  './build_container.sh' 'CPU/2_install_rust'
  './build_container.sh' 'CPU/3_good_setup'
  './build_container.sh' 'CPU/4_python'
  './build_container.sh' 'CPU/5_libtorch'
  './build_container.sh' 'CPU/6_common_python_data'
  './build_container.sh' 'CPU/7_deep_learning'
  './build_container.sh' 'CPU/8_coding_setup'
  exit '0'
#+end_src

** CUDA_UBUNTU
#+begin_src sh :shebang #!/bin/sh :results output :tangle ./build_cuda.sh
  cd "$(dirname -- "${0}")"
  './build_container.sh' 'CUDA_UBUNTU/0_base'
  './build_container.sh' 'CPU/1_install_packages'
  './build_container.sh' 'CPU/2_install_rust'
  './build_container.sh' 'CPU/3_good_setup'
  './build_container.sh' 'CPU/4_python'
  './build_container.sh' 'CUDA_UBUNTU/5_libtorch'
  './build_container.sh' 'CPU/6_common_python_data'
  './build_container.sh' 'CUDA_UBUNTU/7_deep_learning'
  './build_container.sh' 'CPU/8_coding_setup'
  exit '0'
#+end_src

** AMDGPU
#+begin_src sh :shebang #!/bin/sh :results output :tangle ./build_rocm_pytorch.sh
  cd "$(dirname -- "${0}")"
  './build_container.sh' 'AMDGPU_PYT/0_base'
  './build_container.sh' 'CPU/1_install_packages'
  './build_container.sh' 'CPU/2_install_rust'
  './build_container.sh' 'CPU/3_good_setup'
  './build_container.sh' 'CPU/4_python'
  './build_container.sh' 'AMDGPU_PYT/5_libtorch'
  './build_container.sh' 'CPU/6_common_python_data'
  './build_container.sh' 'AMDGPU_PYT/7_deep_learning'
  './build_container.sh' 'CPU/8_coding_setup'
  exit '0'
#+end_src

* Script to run image

** AMDGPU
#+begin_src sh :shebang #!/bin/sh :results output :mkdirp yes :tangle ./run_rocm.sh
  cd "$('dirname' '--' "${0}")"

  # IMAGE_NAME='6_pytorch'
  IMAGE_NAME="$(basename -- "$(realpath -- .)")"
  IMAGE_CMD='zsh'
  PATH_DIR_SRC="$('realpath' '.')"
  PATH_DIR_DST="/data/$('basename' -- "${PATH_DIR_SRC}")"

  podman run \
      '--cap-add=IPC_LOCK' \
      '--ulimit' 'memlock=-1:-1' \
      '--tty' \
      '--interactive' \
      '--rm' \
      '--net' 'host' \
      '--ipc' 'host' \
      '--tmpfs' '/tmp:size=107374182400,exec' \
      '--ulimit' 'memlock=-1' \
      '--ulimit' 'stack=67108864' \
      '--device' '/dev/kfd' \
      '--device' '/dev/dri' \
      '--security-opt' 'seccomp=unconfined' \
      -v "${PATH_DIR_SRC}:${PATH_DIR_DST}" \
      -v "CACHE:/root/cargo/registry" \
      -v "CACHE:/root/.cache" \
      "${IMAGE_NAME}" "${IMAGE_CMD}" \
  ;

      # '--mount' 'type=tmpfs,destination=/tmp,tmpfs-size=107374182400' \
#+end_src

** CPU
#+begin_src sh :shebang #!/bin/sh :results output :mkdirp yes :tangle ./run_cpu.sh
  cd "$('dirname' '--' "${0}")"

  # IMAGE_NAME='6_pytorch'
  IMAGE_NAME="$(basename -- "$(realpath -- .)")"
  IMAGE_CMD='zsh'
  PATH_DIR_SRC="$('realpath' '.')"
  PATH_DIR_DST="/data/$('basename' -- "${PATH_DIR_SRC}")"

  podman run \
      '--cap-add=IPC_LOCK' \
      '--ulimit' 'memlock=-1:-1' \
      '--tty' \
      '--interactive' \
      '--rm' \
      '--net' 'host' \
      '--ipc' 'host' \
      '--tmpfs' '/tmp:size=107374182400,exec' \
      '--ulimit' 'memlock=-1' \
      '--ulimit' 'stack=67108864' \
      '--device' '/dev/dri' \
      '--security-opt' 'seccomp=unconfined' \
      -v "${PATH_DIR_SRC}:${PATH_DIR_DST}" \
      -v "CACHE:/root/cargo/registry" \
      -v "CACHE:/root/.cache" \
      "${IMAGE_NAME}" "${IMAGE_CMD}" \
  ;
#+end_src

** CPU rust build
#+begin_src sh :shebang #!/bin/sh :results output :mkdirp yes :tangle ./run_cpu_rust_build.sh
  cd "$('dirname' '--' "${0}")"

  IMAGE_NAME='2_install_rust'
  # IMAGE_NAME="$(basename -- "$(realpath -- .)")"
  IMAGE_CMD='bash'
  PATH_DIR_SRC="$('realpath' '.')"
  PATH_DIR_DST="/data/$('basename' -- "${PATH_DIR_SRC}")"

  podman run \
      '--cap-add=IPC_LOCK' \
      '--ulimit' 'memlock=-1:-1' \
      '--tty' \
      '--interactive' \
      '--rm' \
      '--net' 'host' \
      '--ipc' 'host' \
      '--tmpfs' '/tmp:size=107374182400,exec' \
      '--ulimit' 'memlock=-1' \
      '--ulimit' 'stack=67108864' \
      '--device' '/dev/dri' \
      '--security-opt' 'seccomp=unconfined' \
      -v "${PATH_DIR_SRC}:${PATH_DIR_DST}" \
      -v "CACHE:/root/cargo/registry" \
      -v "CACHE:/root/.cache" \
      -v "${HOME}/GITHUB:/root/GITHUB" \
      "${IMAGE_NAME}" "${IMAGE_CMD}" \
  ;
#+end_src

** CUDA_UBUNTU
#+begin_src sh :shebang #!/bin/sh :results output :mkdirp yes :tangle ./run_cuda.sh
  cd "$('dirname' '--' "${0}")"

  # IMAGE_NAME='6_pytorch'
  IMAGE_NAME="$(basename -- "$(realpath -- .)")"
  IMAGE_CMD='zsh'
  PATH_DIR_SRC="$('realpath' '.')"
  PATH_DIR_DST="/data/$('basename' -- "${PATH_DIR_SRC}")"

  sudo -A docker run \
      '--cap-add=IPC_LOCK' \
      '--ulimit' 'memlock=-1:-1' \
      '--tty' \
      '--interactive' \
      '--rm' \
      '--net' 'host' \
      '--ipc' 'host' \
      '--tmpfs' '/tmp:size=107374182400,exec' \
      '--ulimit' 'memlock=-1' \
      '--ulimit' 'stack=268435456' \
      '--gpus' 'all,"capabilities=compute,utility,video"' \
      -v "${PATH_DIR_SRC}:${PATH_DIR_DST}" \
      -v "CACHE:/root/cargo/registry" \
      -v "CACHE:/root/.cache" \
      "${IMAGE_NAME}" "${IMAGE_CMD}" \
  ;

      # '--gpus' 'device=0,"capabilities=compute,utility,video"' \
      # '--mount' 'type=tmpfs,destination=/tmp,tmpfs-size=107374182400' \
#+end_src

* Scripts for DGX spark

** Actual dockerfile
llvm libclang-dev clang
#+begin_src conf :mkdirp yes :tangle ./CUDA_DGX/Dockerfile
  FROM nvcr.io/nvidia/pytorch:26.08-py3

  ENV HOME='/root' \
      DEBIAN_FRONTEND='noninteractive' \
      LIBTORCH='/usr/local/lib/python3.12/dist-packages/torch/' \
      CMAKE_EXPORT_COMPILE_COMMANDS=1

  ENV CARGO_HOME="${HOME}/cargo" \
      RUSTUP_HOME="${HOME}/rustup" \
      CMAKE_PREFIX_PATH="${LIBTORCH}/share/cmake" \
      LD_LIBRARY_PATH="${LIBTORCH}/lib:${LD_LIBRARY_PATH}" \
      VIRTUAL_ENV="${HOME}/venv" \
      VIRTUAL_ENV_PROMPT='(venv)'

  ENV PATH="${HOME}/venv/bin:${HOME}/bin:${CARGO_HOME}/bin:${PATH}"

  WORKDIR '/root'
  USER root

  COPY './important_functions.sh' '/root/important_functions.sh'

  RUN \
      --mount=target=/var/lib/apt/lists,type=cache,sharing=locked \
      --mount=target=/var/cache/apt,type=cache,sharing=locked \
      echo 'START apt-get update' \
      && apt-get update \
      && . '/root/important_functions.sh' \
      && apt-get -y install \
          'aria2' \
          'libgl-dev' \
          'libxcb1' \
          'llvm' \
          'libclang-dev' \
          'clang' \
          'ffmpeg' \
      && echo 'DONE apt-get update'

  RUN \
      --mount=target=/root/SHA512SUM,type=cache \
      --mount=target=/root/TMP,type=cache \
      echo 'START Download and install rust' \
      && . "${HOME}/important_functions.sh" \
      && adown \
          'https://sh.rustup.rs' \
          'rustup.rs' \
          '494ddf67a104d5deb49380968ecccd8648ee0d7a3e61a8fa45b4dfd3bdc51ba8777922ed0e1864bf81ba78ce718bd40c55c1835c328df3cd1cd0d17312411460' \
          "${HOME}/rustup-init.sh" \
      && adown \
          'https://ziglang.org/builds/zig-aarch64-linux-0.17.0-dev.2131+d08989840.tar.xz' \
          'zig-aarch64-linux-0.17.0-dev.2131+d08989840.tar.xz' \
          'dc816d391b4a81f5c34a70466716678374b696eadabdf766e13b78b79e931a3c8345f7bfb86cd0db607f5980a8a283dbfd9086f6542fad9d43a8180f2e7a1933' \
          "${HOME}/ZIG/zig-aarch64-linux-0.17.0-dev.2131+d08989840.tar.xz" \
      && cd "${HOME}" \
      && chmod +x 'rustup-init.sh' \
      && './rustup-init.sh' '-y' '--no-modify-path' \
      && rustup default stable \
      && rustup component add rust-src \
      && rustup component add rust-analyzer \
      && rustup component add rustfmt \
      && echo 'DONE Download and install rust' \
      && echo 'START Download and install zig' \
      && cd "${HOME}/ZIG/" \
      && tar -xf './zig-aarch64-linux-0.17.0-dev.2131+d08989840.tar.xz' \
      && cd './zig-aarch64-linux-0.17.0-dev.2131+d08989840' \
      && mv * /usr/local/bin/ \
      && echo 'DONE Download and install zig' ;

  RUN \
      echo 'Start installing python packages' \
      && 'python3' '-m' 'venv' "${HOME}/venv" '--system-site-packages' \
      && . "${HOME}/venv/bin/activate" \
      && python3 -m \
          'pip' 'install' '-U' \
              'albumentationsx' \
              'blake3' \
              'datafusion' \
              'einops' \
              'fastapi[standard]' \
              'fastexcel' \
              'google-auth' \
              'google-cloud-storage' \
              'inotify-simple' \
              'ipywidgets' \
              'jupyter' \
              'jupyterlab' \
              'lancedb' \
              'lightning[extra]' \
              'opencv_contrib_python' \
              'opencv_python' \
              'opencv-python-headless' \
              'openpyxl' \
              'openvino' \
              'pandas' \
              'pillow' \
              'polars' \
              'psutil' \
              'psycopg[binary,pool]' \
              'pylance' \
              'python-calamine' \
              'python-multipart' \
              'ray[serve]' \
              'requests' \
              'safetensors' \
              'scikit-learn' \
              'seaborn' \
              'sentencepiece' \
              'starlette' \
              'timm' \
              'urllib3' \
              'xformers' \
      && echo 'Done installing python packages' ;
#+end_src

  RUN \
      --mount=target=/var/lib/apt/lists,type=cache,sharing=locked \
      --mount=target=/var/cache/apt,type=cache,sharing=locked \
      echo 'START apt-get update' \
      && apt-get update \
      && . '/root/important_functions.sh' \
      && apt-get -y install \
          'aria2' \
          'automake' \
          'bear' \
          'bison' \
          'build-essential' \
          'cmake' \
          'curl' \
          'ffmpeg' \
          'fish' \
          'fzf' \
          'git' \
          'git-lfs' \
          'make' \
          'mold' \
          'squashfs-tools' \
          'unzip' \
          'wget' \
          'yacc' \
          'zip' \
          'zsh' \
          'zstd' \
      && echo 'DONE apt-get update' ;

** build scripts
#+begin_src sh :shebang #!/bin/sh :results output :mkdirp yes :tangle ./build_container_dgx.sh
  cd "$('dirname' -- "${0}")"
  ln -vfs '../shell_functions/important_functions.sh' './'
  cp './important_functions.sh' './CUDA_DGX/'
  cd './CUDA_DGX/'
  IMAGE_NAME='7_deep_learning'
  mkdir -pv -- './build'
  H="$(cat './Dockerfile' './important_functions.sh' | sha512sum | cut -d ' ' -f1)"
  test -e "./build/${H}" && exit '0'
  CMD='sudo -A docker'
  ${CMD} build -t "${IMAGE_NAME}" -f './Dockerfile' . && touch "./build/${H}"
  exit '0'
#+end_src

** run script
#+begin_src sh :shebang #!/bin/sh :results output :mkdirp yes :tangle ./run_cuda_dgx.sh
  cd "$('dirname' '--' "${0}")"

  IMAGE_NAME='7_deep_learning'
  IMAGE_CMD='zsh'
  PATH_DIR_SRC="$('realpath' '.')"
  PATH_DIR_DST="/data/$('basename' -- "${PATH_DIR_SRC}")"

  docker run \
      '--cap-add=IPC_LOCK' \
      '--ulimit' 'memlock=-1:-1' \
      '--tty' \
      '--interactive' \
      '--rm' \
      '--net' 'host' \
      '--ipc' 'host' \
      '--tmpfs' '/tmp:size=107374182400,exec' \
      '--ulimit' 'memlock=-1' \
      '--ulimit' 'stack=268435456' \
      '--gpus' 'all,"capabilities=compute,utility,video"' \
      -v "${PATH_DIR_SRC}:${PATH_DIR_DST}" \
      -v "CACHE:/root/cargo/registry" \
      -v "CACHE:/root/.cache" \
      "${IMAGE_NAME}" "${IMAGE_CMD}" \
  ;
#+end_src

* Script to work

** Actual script
#+begin_src sh :shebang #!/bin/sh :results output :tangle ./.git.sh
  cd "$(dirname -- "${0}")"

  . '../shell_functions/important_functions.sh'

  CLEAN 'CUDA_UBUNTU/5_libtorch/#Dockerfile#'
  CLEAN '.git.sh'
  CLEAN 'README.org~'

  GITADD 'build_container.sh'
  GITADD 'build_cpu.sh'
  GITADD 'build_cuda.sh'
  GITADD 'build_rocm_pytorch.sh'

  GITADD '.gitignore'
  GITADD 'README.org'

  GITADD 'run_cpu_rust_build.sh'
  GITADD 'run_cpu.sh'
  GITADD 'run_cuda.sh'
  GITADD 'run_rocm.sh'

  GITADD 'CPU/0_base/Dockerfile'
  GITADD 'CPU/1_install_packages/Dockerfile'
  GITADD 'CPU/2_install_rust/Dockerfile'
  GITADD 'CPU/3_good_setup/Dockerfile'
  GITADD 'CPU/4_python/Dockerfile'
  GITADD 'CPU/5_libtorch/Dockerfile'
  GITADD 'CPU/6_common_python_data/Dockerfile'
  GITADD 'CPU/7_deep_learning/Dockerfile'
  GITADD 'CPU/8_coding_setup/Dockerfile'

  GITADD 'AMDGPU_PYT/0_base/Dockerfile'
  GITADD 'AMDGPU_PYT/5_libtorch/Dockerfile'
  GITADD 'AMDGPU_PYT/7_deep_learning/Dockerfile'

  GITADD 'CUDA_UBUNTU/0_base/Dockerfile'
  GITADD 'CUDA_UBUNTU/5_libtorch/Dockerfile'
  GITADD 'CUDA_UBUNTU/7_deep_learning/Dockerfile'

  GITADD 'CUDA_DGX/Dockerfile'
  GITADD 'build_container_dgx.sh'
  GITADD 'run_cuda_dgx.sh'

  git commit -m 'Started writing container for DGX spark' ; git status
  "${HOME}/SSH/K/P/setup.sh" ; git push

  exit '0'
#+end_src

** COMMENT Run the script
#+begin_src emacs-lisp :results silent
  (save-buffer)
  (org-babel-tangle)
  (async-shell-command "
      './.git.sh'
  " "log" "err")
#+end_src
